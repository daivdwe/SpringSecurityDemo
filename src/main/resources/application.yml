spring:
  thymeleaf:
    cache: false
  #??: 2. create (test) user
  security:
    user:
      name: user
      password: 42
    #??: 6. configure SSO
    oauth2:
      client:
        registration:
          #TODO: create: https://console.cloud.google.com/apis/credentials
          google:
            #??: secret - do not commit!
            client-id: NOPE
            client-secret: NOPE
server:
  servlet:
    session:
      persistent: false