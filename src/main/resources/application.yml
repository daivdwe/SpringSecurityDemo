spring:
  thymeleaf:
    cache: false
  #??: 2. create (test) user
  security:
    user:
      name: user
      password: 42
    #??: 6. configure SSO
    oauth2:
      client:
        registration:
          # https://console.cloud.google.com/apis/credentials?authuser=1&project=triple-student-420520&supportedpurview=project
          google:
            #??: secret - do not commit!
            client-id: NOPE
            client-secret: NOPE
server:
  servlet:
    session:
      persistent: false